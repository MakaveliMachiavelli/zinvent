# LOCAL SETUP — ZCode Account #2 (100M promo active on YOUR machine)

The VPS connection blocks the promo (ZCode detects shared machine/IP). Your LOCAL machine has the full 100M. Run account #2 HERE.

## Step 1: Get the work
Option A (already on GitHub):
```
git clone https://github.com/MakaveliMachiavelli/zinvent-hub ~/zinvent-local
# or clone any one of the tool repos and pull the rest
```
Option B (from VPS package): unzip zinvent-local-package.tar.gz → `~/zinvent-local/`

## Step 2: Connect account #2
- Open ZCode desktop app on YOUR machine
- Log in with account #2 (the one with 100M promo)
- Start a remote/agent session
- It will deploy the server to YOUR local machine (not VPS)

## Step 3: Paste the nudge
```
Read ~/zinvent-local/ACCOUNT2_NUDGE.md and execute it. Do not ask me anything. Go.
```

## Step 4: What it builds
- Rebuilds Closer per CLOSER-ARTDIRECTION.md (11/10, warm editorial)
- Fixes any broken tools
- Builds 2-3 new PH-freelancer tools
- NO payment walls
- Exhausts 100M on quality

## Step 5: Deploy
The nudge tells it to push to GitHub Pages (MakaveliMachiavelli repos). Your local gh auth handles it. VPS just serves the live URLs.

## Notes
- Local vision (qwen2.5vl) is on the VPS at localhost:11434 — not needed for building
- The 100M is on YOUR machine, not the VPS. That's why it works locally.
